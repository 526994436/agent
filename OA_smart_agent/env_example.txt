# ================================================================
# OA 智能服务台 Agent 微服务 — 环境变量配置示例
# ================================================================
# 复制此文件为 .env，然后填入真实值
# 生产环境建议使用 K8s Secret / Vault 管理敏感配置
# ================================================================

# ─────────────────────────────────────────────
# vLLM（Qwen 3.5-14B-Instruct）配置
# ─────────────────────────────────────────────
VLLM_BASE_URL=http://localhost:8000/v1
VLLM_API_KEY=not-needed
VLLM_MODEL=Qwen/Qwen2.5-14B-Instruct
VLLM_REQUEST_TIMEOUT=60
VLLM_MAX_TOKENS=2000

# ─────────────────────────────────────────────
# Ollama Embedding 配置（用于文本向量化）
# ─────────────────────────────────────────────
EMBEDDING_PROVIDER=ollama
EMBEDDING_MODEL=nomic-embed-text
EMBEDDING_BASE_URL=http://localhost:11434

# ─────────────────────────────────────────────
# Milvus 向量数据库配置（嵌入式 milvus-lite）
# 无需启动独立服务，数据存储在本地文件
# ─────────────────────────────────────────────
MILVUS_DB_PATH=./data/milvus_lite.db
MILVUS_COLLECTION_NAME=oa_chunks

# ─────────────────────────────────────────────
# Java 后端地址配置
# ─────────────────────────────────────────────
JAVA_BACKEND_BASE_URL=http://localhost:8000
JAVA_BACKEND_TIMEOUT=30
JAVA_BACKEND_MAX_RETRIES=3

# ─────────────────────────────────────────────
# Redis 配置（LangGraph Checkpointer）P0
# 启动 Redis：docker run -p 6379:6379 redis:alpine
# 建议生产配置 maxmemory=1gb, maxmemory-policy volatile-lru
# ─────────────────────────────────────────────
REDIS_URL=redis://localhost:6379
REDIS_MAX_CONNECTIONS=50
REDIS_SOCKET_TIMEOUT=10
REDIS_SOCKET_CONNECT_TIMEOUT=5
REDIS_RETRY_ON_TIMEOUT=true
REDIS_SESSION_TTL=1800

# ───────────────────────��─────────────────────
# JWT 鉴权配置（P1：Token 校验）
# 生产必须修改 secret，建议使用随机字符串（32位+）
# ─────────────────────────────────────────────
JWT_SECRET=change-me-in-production-use-random-string
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=480

# ─────────────────────────────────────────────
# CORS 配置（P1：跨域安全）
# 生产必须限制为实际前端域名
# ─────────────────────────────────────────────
CORS_ALLOWED_ORIGINS=["http://localhost:3000"]

# ─────────────────────────────────────────────
# API 限流配置（P1：资源保护）
# 格式：<number>/<unit>，unit 可选 second/minute/hour/day
# ─────────────────────────────────────────────
RATE_LIMIT_PER_USER=60/minute
RATE_LIMIT_PER_IP=120/minute
RATE_LIMIT_BURST=10

# ─────────────────────────────────────────────
# 对话历史持久化配置（P2：审计合规）
# 支持 postgresql / mysql，填写全量连接字符串
# ─────────────────────────────────────────────
# HISTORY_DB_TYPE=postgresql
# HISTORY_DB_URL=postgresql://user:password@localhost:5432/oa_agent_db
# HISTORY_DB_POOL_SIZE=10
# HISTORY_DB_MAX_OVERFLOW=20
HISTORY_SAVE_ASYNC=true

# ─────────────────────────────────────────────
# 监控指标配置（P2：可观测性）
# ─────────────────────────────────────────────
METRICS_ENABLED=true
METRICS_PORT=9090

# ─────────────────────────────────────────────
# 结构化日志配置（P1：可定位）
# format: json | text
# output: stdout | file
# ─────────────────────────────────────────────
LOG_LEVEL=INFO
LOG_FORMAT=json
LOG_OUTPUT=stdout

# ─────────────────────────────────────────────
# 应用全局配置
# ─────────────────────────────────────────────
APP_NAME=OA Smart Agent
DEBUG=false
ENVIRONMENT=production
APP_VERSION=1.0.0

# ─────────────────────────────────────────────
# 多 Agent 架构配置（Phase 1）
# ─────────────────────────────────────────────
MULTI_AGENT_ENABLED=true
DEFAULT_AGENT=router
AGENT_FALLBACK=general

# ─────────────────────────────────────────────
# Tool Calling 配置（Phase 1）
# ─────────────────────────────────────────────
TOOL_CALLING_ENABLED=true
TOOL_TIMEOUT=30
TOOL_MAX_RETRIES=2

# ─────────────────────────────────────────────
# Re-Ranking 配置（Phase 2）
# ─────────────────────────────────────────────
RERANKING_ENABLED=true
RERANKING_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2
RERANKING_INITIAL_TOP_K=50
RERANKING_FINAL_TOP_K=5
RERANKING_USE_MOCK=false

# ─────────────────────────────────────────────
# 知识图谱 RAG 配置（Phase 2）
# ─────────────────────────────────────────────
KNOWLEDGE_GRAPH_ENABLED=true
KG_STORAGE_TYPE=networkx
KG_MAX_HOP=2

# ─────────────────────────────────────────────
# Self-RAG 配置（Phase 2）
# ─────────────────────────────────────────────
SELF_RAG_ENABLED=true
SELF_RAG_MAX_ITERATIONS=3
SELF_RAG_MIN_DOCS=1

# ─────────────────────────────────────────────
# 降级策略配置（Phase 5）
# ─────────────────────────────────────────────
DEGRADE_ENABLED=true
DEGRADE_RAG_THRESHOLD=3
DEGRADE_LLM_THRESHOLD=2
DEGRADE_RECOVERY_THRESHOLD=3

# ─────────────────────────────────────────────
# Celery 异步任务队列配置（Phase 5）
# ─────────────────────────────────────────────
# Broker: RabbitMQ（生产推荐）或 Redis（开发）
# RabbitMQ: amqp://user:pass@host:5672//
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_EXPIRES=86400
CELERY_TASK_ACKS_LATE=true
CELERY_WORKER_PREFETCH_MULTIPLIER=1
CELERY_ASYNC_ENABLED=false

# ─────────────────────────────────────────────
# 多模态 AI 配置（Phase 5）
# 支持：openai（GPT-4o Vision）/ qwen（阿里云百炼）/ claude / mock
# ─────────────────────────────────────────────
MULTIMODAL_ENABLED=false
MULTIMODAL_PROVIDER=mock
# MULTIMODAL_MODEL=gpt-4o  # 可选，默认为各 provider 的推荐模型
