# 智能服务台 Agent 微服务 — Python 依赖清单（生产级）
# ============================================================
# 安装命令：pip install -r requirements.txt
# ============================================================

# ─────────────────────────────────────────────
# LangGraph >= 1.1.0（核心框架）
# 提供 StateGraph、RedisSaver、interrupt_before 等关键特性
# ─────────────────────────────────────────────
langgraph>=1.1.0

# ─────────────────────────────────────────────
# Redis 客户端（P0：连接池配置）
# 用于 LangGraph Checkpointer 持久化，支持多进程/多实例共享状态
# ─────────────────────────────────────────────
redis>=5.0.0

# ─────────────────────────────────────────────
# LangChain OpenAI 集成（LLM + Embedding）
# ─────────────────────────────────────────────
langchain-openai>=0.3.0
langchain-core>=0.3.0

# ─────────────────────────────────────────────
# FastAPI Web 框架
# ─────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# ─────────────────────────────────────────────
# 数据校验
# ─────────────────────────────────────────────
pydantic>=2.9.0
pydantic-settings>=2.6.0

# ─────────────────────────────────────────────
# 向量数据库客户端（Milvus 嵌入式 milvus-lite）
# 无需启动独立服务，使用本地文件存储
# ─────────────────────────────────────────────
pymilvus>=2.4.2

# ─────────────────────────────────────────────
# HTTP 客户端（P0：Java 后端调用，含重试/熔断）
# ─────────────────────────────────────────────
httpx>=0.28.0

# ─────────────────────────────────────────────
# 环境变量管理
# ─────────────────────────────────────────────
python-dotenv>=1.0.0

# ─────────────────────────────────────────────
# 日志增强（P1：结构化 JSON 日志）
# ─────────────────────────────────────────────
loguru>=0.7.0

# ─────────────────────────────────────────────
# JWT 鉴权（P1：Token 校验）
# ─────────────────────────────────────────────
PyJWT>=2.9.0

# ─────────────────────────────────────────────
# 数据库 ORM（P2：对话历史持久化）
# 根据需要启用（postgresql 或 mysql）
# ─────────────────────────────────────────────
# SQLAlchemy>=2.0.0
# asyncpg>=0.29.0     # PostgreSQL 异步驱动
# aiomysql>=0.2.0     # MySQL 异步驱动

# ─────────────────────────────────────────────
# Prometheus 指标暴露（供外部 Prometheus 抓取）
# ─────────────────────────────────────────────
prometheus-client>=0.20.0

# ─────────────────────────────────────────────
# Docker / K8s 健康检查辅助
# ─────────────────────────────────────────────
python-multipart>=0.0.9

# ─────────────────────────────────────────────
# LangChain 工具调用（Phase 1：原生 Tool Calling）
# ─────────────────────────────────────────────
langchain>=0.3.0
langchain-core>=0.3.0

# ─────────────────────────────────────────────
# Cross-Encoder 重排序（Phase 2：Re-Ranking）
# ─────────────────────────────────────────────
sentence-transformers>=2.2.0

# ─────────────────────────────────────────────
# ABAC 权限模型（Phase 2：ABAC + Milvus 过滤）
# 替代 Neo4j 图数据库，使用 Milvus Expr 标签过滤实现权限控制
# ─────────────────────────────────────────────
# networkx>=3.1  # 如需复杂图算法可启用

# ─────────────────────────────────────────────
# 数据库连接池（Phase 3：对话历史持久化）
# ─────────────────────────────────────────────
# SQLAlchemy>=2.0.0
# asyncpg>=0.29.0     # PostgreSQL 异步驱动
# aiomysql>=0.2.0     # MySQL 异步驱动

# ─────────────────────────────────────────────
# 统计工具（Phase 4：A/B 测试统计）
# ─────────────────────────────────────────────
scipy>=1.11.0

# ─────────────────────────────────────────────
# FastMCP（MCP 协议实现）
# 用于标准化调用 Java 后端 API
# ─────────────────────────────────────────────
fastmcp>=0.1.0

# ─────────────────────────────────────────────
# Celery 异步任务队列（Phase 5）
# Broker: RabbitMQ（生产）或 Redis（开发）
# 用于异步执行 LangGraph 任务，支持任务追踪和崩溃恢复
# ─────────────────────────────────────────────
celery>=5.3.0

# ─────────────────────────────────────────────
# 多模态 AI（Phase 5：多模态 Agent）
# 支持 GPT-4o Vision / Qwen-VL / Claude 3
# 用于图片识别、发票提取、故障诊断
# ─────────────────────────────────────────────
# openai>=1.0.0           # OpenAI SDK（已包含在 langchain-openai）
# anthropic>=0.18.0       # Claude SDK（可选，如使用 Claude 多模态）
# dashscope>=2.0.0        # 阿里云百炼 SDK（可选，如使用 Qwen-VL）
pillow>=10.0.0            # 图片处理

# ─────────────────────────────────────────────
# 重排序模型（Phase 5）
# 支持 Cross-Encoder / BGE-Reranker / Cohere
# ─────────────────────────────────────────────
# sentence-transformers>=2.2.0  # 已包含
cohere>=4.0.0              # Cohere Rerank API（可选）

# =============================================================
# ▼▼▼  LlamaIndex 文档 ETL 流水线（data_ingestion 模块）  ▼▼▼
# =============================================================

# ─────────────────────────────────────────────
# LlamaIndex 核心（NodeParser、Schema、StorageContext 等）
# ─────────────────────────────────────────────
llama-index-core>=0.12.0

# ─────────────────────────────────────────────
# LlamaIndex 官方文件 Reader
# 提供 PyMuPDFReader / DocxReader / FlatReader /
#        PandasExcelReader / PandasCSVReader 等
# ─────────────────────────────────────────────
llama-index-readers-file>=0.4.0

# ─────────────────────────────────────────────
# LlamaIndex Milvus 向量存储集成
# ─────────────────────────────────────────────
llama-index-vector-stores-milvus>=0.4.0

# ─────────────────────────────────────────────
# Ollama Embedding（Settings.embed_model）
# ─────────────────────────────────────────────
langchain-ollama>=0.2.0
llama-index-embeddings-ollama>=0.2.0

# ─────────────────────────────────────────────
# LlamaIndex BM25 检索器（底层使用 rank_bm25）
# ─────────────────────────────────────────────
llama-index-retrievers-bm25>=0.3.0
rank-bm25>=0.2.0

# ─────────────────────────────────────────────
# LlamaIndex PostProcessor（重排序）
# 包含 CohereRerank / SentenceTransformerReranker / FixedRecencyPostprocessor
# ─────────────────────────────────────────────
llama-index-postprocessor-cohere-rerank>=0.3.0
llama-index-postprocessor-colbert-reranker>=0.3.0

# ─────────────────────────────────────────────
# LlamaIndex Query Pipeline（高级链路编排，可选）
# ─────────────────────────────────────────────
# llama-index-core >=0.12.0 已包含 QueryPipeline

# ─────────────────────────────────────────────
# PDF 解析 - Unstructured.io (hi_res 策略，专为 LLM/RAG 优化)
# strategy="hi_res" 自动检测文档布局、表格、图像等元素
# ─────────────────────────────────────────────
unstructured>=0.16.0
unstructured[pdf]>=0.16.0

# ─────────────────────────────────────────────
# Word 解析底层依赖（DocxReader 需要 docx2txt）
# ─────────────────────────────────────────────
docx2txt>=0.8              # DocxReader 后端
python-docx>=1.1.0         # 备用 Word 解析

# ─────────────────────────────────────────────
# Excel / CSV 解析底层依赖
# ─────────────────────────────────────────────
pandas>=2.0.0
openpyxl>=3.1.0            # xlsx 读写
xlrd>=2.0.1                # xls 读取

# ─────────────────────────────────────────────
# PowerPoint 解析底层依赖
# ─────────────────────────────────────────────
python-pptx>=0.6.21        # PPTX 解析

